.DS_Store
node_modules/

# Next.js / build
.next/
out/
build/
*.tsbuildinfo
.vercel/

# Prisma Client ที่ generate เอง (CI รัน `pnpm db:generate` ก่อน verify)
lib/generated/

# env & secrets
.env
.env.*
!.env.example

# Claude Code — ค่าเฉพาะเครื่อง
CLAUDE.local.md
.claude/settings.local.json

# Orchestrator runtime state (ห้าม commit — ระบบสร้างใหม่เอง)
orchestrator/queue/
orchestrator/logs/
orchestrator/.token
orchestrator/.run.lock

# misc
*.log
/tmp/
coverage/
_to_delete/
