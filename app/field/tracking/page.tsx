import { TrackingTab } from '@/components/field/tracking-tab'

/** แท็บ "กำลังติดตาม" (`41` §7.5) — จัดกลุ่มตามวัน + สลับลำดับ + ปุ่ม 3 สถานะ */
export default function FieldTrackingPage() {
  return <TrackingTab />
}
